import React from 'react';
import {Section, theme} from '../assets/styles/theme';
import {CheckBoxButton} from '../components/elements/Buttons';

const onPress = () => {
  // Alert.alert('hello');
};
export default function RefrigerPage() {
  return (
    <Section>
      <CheckBoxButton width="30%">dkssud</CheckBoxButton>
    </Section>
  );
}
